{
  "name": "dmagni-project",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "install:all": "npm install --prefix frontend && npm install --prefix backend"
  },
  "dependencies": {
    "next": "16.1.6",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  }
}
